# Uber-CV
Subscribe to image topic from realsense camera, convert image with two colored balls to angle, publish angle.

For adjusting CV parameters, run `setup.sh` or `roslaunch uber-cv setup.launch`.

